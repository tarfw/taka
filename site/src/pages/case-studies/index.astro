---
import Layout from '../../layouts/Layout.astro';
import { caseStudies, getFeaturedCaseStudy } from '../../data/caseStudies';

const featured = getFeaturedCaseStudy();
const rest = caseStudies.filter((c) => !c.featured);
---

<Layout
  title="Case Studies"
  hasHero={true}
  headerSolid={true}
  description="Case studies from schools, hospitals, research laboratories and government institutions across the UAE supplied and installed by TAKA Scientific."
>
  <section class="page-hero" data-view>
    <div class="container">
      <span class="eyebrow t--fade">Case studies</span>
      <h1 class="page-hero-title" data-splitting="words">Real projects, real handovers.</h1>
      <p class="page-hero-sub t--fade t--delay_200">
        The equipment we've supplied and installed for UAE schools, hospitals, research
        laboratories and government facilities — and the results each project delivered.
      </p>
      <div class="hero-actions t--fade t--delay_400">
        <a href="/contact" class="hero-btn">TALK TO OUR TEAM</a>
      </div>
    </div>
  </section>

  {featured && (
    <section class="featured-section" data-view>
      <div class="container">
        <a href={`/case-studies/${featured.slug}`} class="featured-card t--fade">
          <div class="featured-visual clip--top_right">
            <img src={featured.image} alt={featured.imageAlt} class="featured-img" loading="lazy" />
            <span class="featured-tag">{featured.sector}</span>
          </div>
          <div class="featured-content">
            <div class="featured-meta">
              <span class="featured-org">{featured.organization}</span>
              <span class="featured-eyebrow">{featured.emirate} &middot; {featured.timeframe}</span>
            </div>
            <h2 class="featured-headline">{featured.headline}</h2>
            <p class="featured-quote">&ldquo;{featured.quote}&rdquo;</p>
            <div class="featured-author">
              <p class="featured-name">{featured.author}</p>
              <p class="featured-role">{featured.role}</p>
            </div>
            <span class="featured-cta">Read case study
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
            </span>
          </div>
        </a>
      </div>
    </section>
  )}

  <section class="list-section" data-view>
    <div class="container">
      <div class="list-grid">
        {rest.map((c, i) => (
          <a href={`/case-studies/${c.slug}`} class="study-card t--fade t--delay_{(i % 2) * 200 + 100}">
            <div class="study-card-img-wrap">
              <img src={c.image} alt={c.imageAlt} class="study-card-img" loading="lazy" />
            </div>
            <div class="study-card-top">
              <span class="study-sector">{c.sector}</span>
              <span class="study-meta">{c.emirate}</span>
            </div>
            <h3 class="study-org">{c.organization}</h3>
            <p class="study-headline">{c.headline}</p>
            <p class="study-summary">{c.summary}</p>
            <div class="study-stats">
              {c.stats.map((s) => (
                <div class="study-stat">
                  <span class="study-stat-value">{s.value}</span>
                  <span class="study-stat-label">{s.label}</span>
                </div>
              ))}
            </div>
            <span class="study-cta">Read case study
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
            </span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <section class="ice-cta" data-view>
    <h2 class="t--fadedown">Every project starts with a conversation.</h2>
    <p class="t--fade t--delay_200">Tell us about your facility, your requirements and your timeline. We handle the rest.</p>
    <a href="/contact" class="ice-btn ice-btn-white t--fade t--delay_400"><span>Talk to our team</span></a>
  </section>

  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "ItemList",
    "itemListElement": caseStudies.map((c, i) => ({
      "@type": "ListItem",
      "position": i + 1,
      "item": {
        "@type": "CreativeWork",
        "name": `${c.organization} — ${c.headline}`,
        "url": `https://taka.ae/case-studies/${c.slug}`,
        "creator": { "@type": "Organization", "name": "TAKA Scientific" },
      },
    })),
  })} />
</Layout>

<style>
  .page-hero {
    padding: calc(var(--nav-height) + 3.5rem) 0 3.5rem 0;
    background: #fff;
    position: relative;
  }
  .page-hero .container {
    position: relative;
    z-index: 1;
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    text-align: left;
  }
  .page-hero .eyebrow {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: #1a365d;
    margin-bottom: 0.875rem;
    display: block;
    text-align: left;
  }
  .page-hero-title {
    font-family: var(--font-sans);
    font-size: clamp(2rem, 4.5vw, 3.5rem);
    font-weight: 600;
    letter-spacing: -0.025em;
    color: #1a365d;
    margin-bottom: 1rem;
    line-height: 1.12;
    max-width: 850px;
    text-align: left;
  }
  .page-hero-sub {
    font-family: var(--font-sans);
    font-size: clamp(0.95rem, 1.3vw, 1.08rem);
    color: #1a365d;
    opacity: 0.8;
    line-height: 1.6;
    max-width: 560px;
    text-align: left;
    margin-bottom: 1.5rem;
  }
  .hero-actions {
    display: flex;
    align-items: center;
    justify-content: flex-start;
  }
  .hero-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    height: 38px;
    padding: 0 32px;
    background: #1a365d;
    color: #ffffff;
    border: 1px solid #1a365d;
    text-decoration: none;
    transition: background 0.15s ease, color 0.15s ease;
  }
  .hero-btn:hover {
    background: #ffffff;
    color: #1a365d;
  }

  /* ═══ Featured card ═══ */
  .featured-section {
    padding: 4rem 0 2rem;
  }
  .featured-card {
    display: grid;
    grid-template-columns: 42% 1fr;
    border: 1px solid var(--color-border);
    border-radius: 6px;
    overflow: hidden;
    background: #fff;
    text-decoration: none;
    transition: border-color 0.2s;
  }
  .featured-card:hover { border-color: var(--greige-lightest); }
  .featured-visual {
    position: relative;
    min-height: 340px;
    overflow: hidden;
    background: #1a365d;
  }
  .featured-img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  .featured-tag {
    position: absolute;
    top: 1.25rem;
    left: 1.25rem;
    padding: 0.3rem 0.65rem;
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: #fff;
    background: rgba(26,54,93,0.82);
    backdrop-filter: blur(6px);
    border-radius: 4px;
  }
  .featured-content {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 2.5rem 2.25rem;
  }
  .featured-meta { display: flex; flex-direction: column; gap: 0.15rem; margin-bottom: 1.25rem; }
  .featured-org {
    font-size: 0.95rem;
    font-weight: 600;
    color: #111;
  }
  .featured-eyebrow {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-text-muted);
  }
  .featured-headline {
    font-family: var(--font-serif);
    font-size: clamp(1.4rem, 2.5vw, 2rem);
    font-weight: 400;
    line-height: 1.2;
    letter-spacing: -0.02em;
    color: #111;
    margin-bottom: 1.25rem;
  }
  .featured-quote {
    font-size: 1rem;
    line-height: 1.7;
    color: var(--color-text-secondary);
    margin-bottom: 1.5rem;
  }
  .featured-author { margin-bottom: 1.75rem; }
  .featured-name {
    font-family: var(--font-serif);
    font-size: 1.05rem;
    color: #111;
  }
  .featured-role {
    font-size: 0.9rem;
    color: var(--color-text-muted);
  }
  .featured-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    align-self: flex-start;
    padding: 0.55rem 0.9rem;
    background: #1a365d;
    color: #fff;
    font-size: 0.875rem;
    font-weight: 500;
    border-radius: 4px;
    transition: background 0.15s;
  }
  .featured-card:hover .featured-cta { background: #163052; }

  /* ═══ Cards grid ═══ */
  .list-section { padding: 2rem 0 4rem; }
  .list-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1px;
    background: var(--color-border);
    border: 1px solid var(--color-border);
    border-radius: 6px;
    overflow: hidden;
  }
  .study-card {
    display: flex;
    flex-direction: column;
    background: var(--cream);
    padding: 2rem;
    text-decoration: none;
    transition: background 0.15s;
  }
  .study-card:hover { background: var(--cream-darker); }
  .study-card-img-wrap {
    margin: -2rem -2rem 1.75rem;
    height: 190px;
    overflow: hidden;
    background: #1a365d;
  }
  .study-card-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.3s;
  }
  .study-card:hover .study-card-img { transform: scale(1.03); }
  .study-card-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 1.25rem;
  }
  .study-sector {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: #1a365d;
    background: #e8edf5;
    padding: 0.3rem 0.6rem;
    border-radius: 4px;
  }
  .study-meta {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--color-text-muted);
  }
  .study-org {
    font-size: 0.95rem;
    font-weight: 600;
    color: #111;
    margin-bottom: 0.5rem;
  }
  .study-headline {
    font-family: var(--font-serif);
    font-size: 1.3rem;
    font-weight: 400;
    line-height: 1.3;
    letter-spacing: -0.02em;
    color: #111;
    margin-bottom: 0.75rem;
  }
  .study-summary {
    font-size: 0.9rem;
    line-height: 1.7;
    color: var(--color-text-secondary);
    margin-bottom: 1.75rem;
  }
  .study-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
    border-top: 1px solid var(--color-border);
    padding-top: 1.5rem;
    margin-top: auto;
    margin-bottom: 1.5rem;
  }
  .study-stat { display: flex; flex-direction: column; gap: 0.25rem; }
  .study-stat-value {
    font-family: var(--font-serif);
    font-size: 1.6rem;
    line-height: 1;
    color: #111;
  }
  .study-stat-label {
    font-size: 0.78rem;
    line-height: 1.35;
    color: var(--color-text-muted);
  }
  .study-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    align-self: flex-start;
    font-size: 0.875rem;
    font-weight: 500;
    color: #1a365d;
    transition: gap 0.15s;
  }
  .study-card:hover .study-cta { gap: 0.75rem; }

  @media (max-width: 900px) {
    .featured-card { grid-template-columns: 1fr; }
    .featured-visual { min-height: 240px; }
    .list-grid { grid-template-columns: 1fr; }
  }
  @media (max-width: 768px) {
    .page-hero { padding: calc(var(--nav-height) + 1.5rem) 0 2.5rem 0; }
    .page-hero-title { font-size: 2rem; }
    .featured-section { padding: 2.5rem 0 1.5rem; }
    .featured-content { padding: 1.75rem 1.5rem; }
    .study-card { padding: 1.5rem; }
    .study-stats { grid-template-columns: 1fr; gap: 1rem; }
  }
/* ═══ CONTACT CTA — ice-cta design (matches analytical) ═══ */
  .ice-cta {
    --ice-pad: 24px;
    background: #fff;
    color: #1a365d;
    min-height: 100svh;
    justify-content: center;
    padding: 80px var(--ice-pad);
    text-align: center;
    flex-direction: column;
    align-items: center;
    gap: 20px;
    display: flex;
    position: relative;
    z-index: 1;
  }
  .ice-cta h2 {
    font-family: var(--font-sans);
    letter-spacing: -0.01em;
    font-size: 24px;
    line-height: 1.15;
    font-weight: 600;
    margin: 0;
    max-width: 20ch;
  }
  .ice-cta p {
    font-family: var(--font-sans);
    font-size: 15px;
    line-height: 1.5;
    margin: 0;
    opacity: 0.8;
    max-width: 44ch;
  }
  .ice-cta .ice-btn,
  .ice-cta .ice-btn:hover {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    white-space: nowrap;
    font-family: var(--font-mono);
    letter-spacing: 0.01em;
    text-transform: uppercase;
    height: 32px;
    padding: 0 36px;
    border: 1px solid #1a365d;
    background: #1a365d;
    color: #ffffff;
    font-size: 12px;
    line-height: 1.4;
    cursor: pointer;
    text-decoration: none;
    transition: background 0.1s ease-out, color 0.1s ease-out, border-color 0.1s ease-out;
  }
  .ice-cta .ice-btn:hover {
    background: #ffffff;
    color: #1a365d;
  }
  .ice-cta .ice-btn span { pointer-events: none; }
  @media (min-width: 1024px) {
    .ice-cta { gap: 30px; }
    .ice-cta h2 {
      letter-spacing: -0.02em;
      font-size: 64px;
      line-height: 1.05;
    }
  }
  @media (max-width: 480px) {
    .ice-cta { --ice-pad: 16px; }
  }
</style>
